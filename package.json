{
  "name": "snowflake-mcp-server",
  "version": "1.0.0",
  "description": "Snowflake MCP server for BenBetter.ai benefit guides database",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "echo 'Build complete'",
    "start": "node server.js --http --port 10000"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}